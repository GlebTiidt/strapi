'use strict';

/**
 * events-galleries-en router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::events-galleries-en.events-galleries-en');
