'use strict';

/**
 * events-galleries-en service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::events-galleries-en.events-galleries-en');
